# cloudflare-whatsmyip

A Cloudflare Worker based implementation that serves the client ip as seen by a web server, hosted on https://ip.rootnet.in

## Usage
`curl https://ip.rootnet.in`

or

`curl https://ip.rootnet.in/json`

